<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28*"/>
            <RowDefinition Height="139*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="16*"/>
            <ColumnDefinition Width="83*"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="out_block" Grid.Column="1" Grid.Row="0"/>
        <ListBox Grid.Column="0" Grid.Row="1">
            <TextBlock Text="n"/>
            <TextBox x:Name="N"  Margin="5"/>
            <TextBox x:Name="M" Margin="5" />

        </ListBox>

        <Button  Content="Start Button" Grid.Column="0" Click="OperationOnClick"/>
        <ListBox x:Name ="KindaConsol" Grid.Column="1" Grid.Row="1">
            "All is ready"
        </ListBox>

    </Grid>
</Window>
